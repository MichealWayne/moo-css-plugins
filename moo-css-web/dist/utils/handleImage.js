"use strict";
/**
 * handle image base64
 */
/*
export default function handleImgBase64 (base64: string) {
   let base64Data = base64.replace(/^data:image\/\w+;base64,/, '');
   let dataBuffer = new Buffer(base64Data, 'base64');
   return dataBuffer;
}*/ 
